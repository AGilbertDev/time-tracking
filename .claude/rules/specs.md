---
paths:
  - 'docs/specs/**'
---

Before writing or amending a spec, load `/workflow:spec` and follow its template.

One spec per feature, kept current, under 150 lines. Acceptance criteria are numbered `AC1`, `AC2`, and each one is observable. Name the test that proves a criterion after it, so the pull request report can say which criteria have a passing test.
