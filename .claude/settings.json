{
  "$comment": "Committed on purpose. A clone of this project gets the same workflow, so the plugins are enabled at project scope rather than per machine. The conventions, the pipeline, and the hooks all arrive with them.",
  "extraKnownMarketplaces": {
    "agilbertdev": {
      "source": { "source": "github", "repo": "AGilbertDev/claude-plugins" }
    }
  },
  "enabledPlugins": {
    "workflow@agilbertdev": true,
    "nuxt-conventions@agilbertdev": true
  }
}
