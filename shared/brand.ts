// The product's public name, shown to users in transactional email (the sender identity and the
// message copy). Kept in one place so the brand cannot drift between the sender name and the body,
// and so "the application" is never left vague. The name a recipient's own name cannot convey.
export const APP_NAME = 'Time Tracking App'
